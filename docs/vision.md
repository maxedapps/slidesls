# Vision

slidesls is an agent-primary authoring tool for web slide decks.

- Generated output is plain HTML/CSS/JS.
- The npm package is an authoring helper, not a runtime dependency.
- Registry items are copied into decks and may be edited.
- CLI output should be deterministic and JSON-friendly for agents.
- Browser/snapshot workflows are optional so the base package stays lightweight.
