# Technical Walkthrough

Slide template for explaining project structure plus a small CLI/process excerpt.

Copy dependencies first: `utilities/layout`, `components/file-tree`, and `components/terminal`.
