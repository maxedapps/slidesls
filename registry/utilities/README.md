# Utilities

Reusable classes that work anywhere in a deck.

Current items:

- `layout/` — stacks, clusters, grids, centering, fill, and frame helpers.

Utilities should not depend on hidden parent or slide-layout structure.
