{
  "name": "archetypes/big-stat",
  "type": "ls:archetype",
  "title": "Big stat",
  "description": "One to three numbers at display size with quiet labels and one grounding context line. Scale contrast does the work; no boxes, no chart junk.",
  "intent": ["show-data", "prove"],
  "tags": ["archetype", "stat", "numbers"],
  "useCases": [
    "landing the headline metrics of a quarter or launch",
    "proving a claim with one unmissable number"
  ],
  "files": [],
  "registryDependencies": ["core/base", "layouts/core", "components/stat"],
  "dependencies": [],
  "devDependencies": [],
  "docs": "registry/archetypes/big-stat/README.md",
  "agentLevel": "starter",
  "contract": {
    "stats": {
      "min": 1,
      "max": 3,
      "description": "1–3 numbers; four numbers is a dashboard, not a headline."
    },
    "statValue": {
      "maxChars": 6,
      "description": "Short display strings only — \"3.4×\", \"99.99%\", \"12ms\"."
    },
    "statLabel": { "maxWords": 8 },
    "context": {
      "min": 0,
      "max": 1,
      "maxWords": 16,
      "description": "One grounding line: where the numbers come from and why they can be trusted."
    }
  },
  "motion": {
    "default": "stagger",
    "notes": "The entrance stagger lands the numbers one after another; do not add data-step to stats."
  },
  "icons": { "guidance": "No icons next to numbers; the value is the graphic." },
  "composition": {
    "layoutBehavior": "fills-area",
    "useWhen": [
      "one to three numbers carry the argument on their own",
      "the audience should remember a value, not a shape"
    ],
    "avoidWhen": [
      "four or more metrics — use archetypes/dashboard before the values shrink",
      "the numbers need a trend or distribution to be honest — use components/chart"
    ],
    "alternatives": [
      { "when": "the numbers need customer words behind them", "use": "archetypes/evidence" },
      { "when": "an operational spread of metrics", "use": "archetypes/dashboard" }
    ]
  },
  "authoring": {
    "attributes": [
      {
        "name": "data-ls-archetype",
        "scope": "section.ls-slide",
        "value": "big-stat",
        "description": "Marks the slide for contract linting and deck scorecards."
      }
    ],
    "usage": [
      "Wrap units in <em> (\"12<em>ms</em>\") so the number stays the loudest thing on the slide.",
      "Use ls-stat__delta only when the direction of change is the story.",
      "The context line earns trust — say where the numbers come from, not another adjective."
    ]
  },
  "snippets": [{ "label": "Basic", "path": "registry/archetypes/big-stat/snippets/basic.html" }]
}
