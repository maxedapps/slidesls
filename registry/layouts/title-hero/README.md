# layouts/title-hero

Opening slide layout with a large title area and a visual/icon panel.

Dependencies:

- `core/base`
- `components/badge` when using the badge row

Use `.ls-layout-title-hero` on the slide and compose content with `.ls-title-hero__content`, `.ls-title-hero__badges`, and `.ls-title-hero__visual`.
