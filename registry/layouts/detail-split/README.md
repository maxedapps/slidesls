# layouts/detail-split

Asymmetric detail layout for explanation slides.

Dependencies:

- `core/base`

Use `.ls-layout-detail-split` on the slide, then place the main visual in `.ls-detail-split__visual` and supporting content in `.ls-detail-split__stack`.
