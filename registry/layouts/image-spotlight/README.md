# Image Spotlight

A copyable layout primitive for image spotlight slides. Main class: `.ls-image-spotlight` or layout wrapper `.ls-layout-image-spotlight` where documented by the CSS.

## Copy

Copy `image-spotlight.css` after `core/base` styles. Use the classes documented here in your slide markup.
