# Asymmetric Feature

A copyable layout primitive for asymmetric feature slides. Main class: `.ls-asymmetric-feature` or layout wrapper `.ls-layout-asymmetric-feature` where documented by the CSS.

## Copy

Copy `asymmetric-feature.css` after `core/base` styles. Use the classes documented here in your slide markup.
