# Components

Reusable slide building blocks.

Current items:

- `badge/` — compact labels.
- `card/` — flat feature/explanation cards.
- `diagram/` — simple flow and file-tree visuals.
- `callout/` — highlighted note, warning, insight, or takeaway.
- `metric/` — single KPI/stat block.
- `stat-grid/` — grouped metrics.
- `bullet-list/` — styled bullets, checklists, and process lists.
- `code-block/` — presentation-ready code snippets without a syntax highlighter dependency.
- `media-frame/` — framed screenshots, images, videos, diagrams, and captions.
- `quote/` — testimonial, pull quote, or cited statement.
- `table/` — semantic presentation table styling.
- `timeline/` — reusable sequence component for milestones and processes.
- `numbered-step/` — step cards/items for tutorials, agendas, and workflows.
- `progress/` — native or custom accessible progress bars/meters.
- `logo-strip/` — logo, partner, tool, or ecosystem rows.
- `highlight-text/` — inline emphasis for phrases, values, and notes.
- `divider/` — internal separator for dense slides.

Components depend on `core/base` and should remain small, semantic, and copyable.
