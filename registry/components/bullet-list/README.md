# Bullet List

A dependency-free component primitive for bullet list content. Use `.ls-bullet-list` and child classes from `bullet-list.css`.

## Copy

Copy `bullet-list.css` after `core/base` styles. Use the classes documented here in your slide markup.
